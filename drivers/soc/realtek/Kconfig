config RTD119X
	bool "Realtek RTD119X SoC family specific drivers"
	depends on ARCH_RTD119X
	default y
	help
	  Realtek RTD119X SoC family specific support.

	  If unsure, say N.

if RTD119X
source "drivers/soc/realtek/rtd119x/Kconfig"
source "drivers/soc/realtek/common/Kconfig"
endif

config RTK_PLATFORM_FPGA
	def_bool n

# Selected by COMMON_CLK_REALTEK and other drivers; must not live only
# inside the SoC-specific if RTD* blocks or Kconfig reports unmet deps.
config RTK_MMIO
	bool
	select REGMAP_MMIO

config RTK_REGISTER_TRACKER
	bool
